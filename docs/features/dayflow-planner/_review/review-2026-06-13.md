# Review — dayflow-planner

**Date:** 2026-06-13  
**Reviewer:** independent `sdd-reviewer` (clean context)  
**Scope:** `git diff 72d9d58..HEAD` — 121 files, +14,259 lines  
**Tests at review:** `npm test -- --run` — 11 files, 60 tests, all green  

## Gate result

**CHANGES REQUESTED**

Core domain logic is implemented and integration-tested, but stage-1 gaps in UI/e2e test coverage, offline PWA verification, and storage-error handling block ship. Stage-2 quality items also require fixes per user resolution.

---

## Findings and resolutions

### Stage 1 — spec / AC compliance

| # | Finding | Location | AC | Verdict |
|---|---|---|---|---|
| S1-01 | Offline PWA unverified — no e2e/smoke test for install → offline → read/write | `vite.config.ts:12-38`, `src/main.tsx:7` | AC-13 | **Fix now** |
| S1-02 | Spec test-plan tiers missing — no e2e-through-UI, visual-regression, or load suites | repo root | spec §5 CI | **Fix now** |
| S1-03 | AC-12 progress + expandable checklist untested in UI | `src/pages/long-term/LongTermPage.tsx:96-149` | AC-12 | **Fix now** |
| S1-04 | AC-01 focus retention after quick-add untested | `src/pages/dashboard/QuickAddForm.tsx:14-26` | AC-01 | **Fix now** |
| S1-05 | AC-18 long-term navigation/progress coverage thin | `src/app/App.test.tsx:23-26` | AC-18 | **Fix now** |
| S1-06 | UI ACs lack component/e2e tests per test plan (daily CRUD, rollover, settings) | `DailyTaskList.tsx`, `SettingsPage.tsx` | AC-04–04c, 06–07, 09*, 11*, 12*, 15 | **Fix now** |
| S1-07 | IndexedDB open failure silent — no error banner, quick-add no-ops | `src/app/providers/PlannerProvider.tsx:62-64` | spec §5 edge case | **Fix now** |
| S1-08 | Post-install export hint not implemented | dashboard pages | spec §8 OQ #2 | **Defer** → §8 (owner: PM, due: before ship) |
| S1-09 | Empty-state recovery banner untested | `src/app/components/EmptyStateBanner.tsx:9-15` | T14 DoD | **Defer** → §8 (owner: Tech Lead, due: with e2e suite) |

### Stage 2 — quality

| # | Finding | Location | AC | Verdict |
|---|---|---|---|---|
| S2-01 | Latest long-term tie-break nondeterministic on equal `createdAt` | `long-term-task-repository.ts:39-44` | AC-03 | **Fix now** |
| S2-02 | AC-17 step ordering not tested | `step-repository.ts:28-34` | AC-17 | **Fix now** |
| S2-03 | AC-16 unrecognized-prefix branch untested | `src/shared/lib/quick-add.ts:23` | AC-16 | **Fix now** |
| S2-04 | Merge dedup uses global id Set, not per-store | `src/features/backup/backup.ts:140-177` | AC-11, ADR-0003 | **Fix now** |
| S2-05 | Backup validation shallow — no per-record shape check | `src/features/backup/backup.ts:47-54` | AC-19 | **Fix now** |
| S2-06 | Blank step edit error unhandled in UI | `src/pages/long-term/LongTermPage.tsx:182-186` | AC-14 pattern | **Fix now** |
| S2-07 | FSD same-layer import — rollover → task-crud | `src/features/rollover/rollover-actions.ts:7-11` | sad §5 | **Fix now** |
| S2-08 | Contract tier underspecified — no full v1 schema fixture | `src/features/backup/backup.test.ts:39-57` | AC-10 | **Fix now** |
| S2-09 | Load NFRs unautomated (p95 quick-add, dashboard load) | spec §6 | n/a | **Fix now** |

---

## What passes

- **Domain flows** at service layer: prefix capture (AC-01–03/05–06/08/16/20), rollover (AC-09b/09f/09/09c–09g), task CRUD (AC-04/04b/04c/07b/12b–12h/14), backup export/import (AC-10–11b/15/19).
- **ADR-0002** IndexedDB schema matches `data-model.md`.
- **Merge summary** (`added`/`skipped`) satisfies spec §6.1 abuse case at service + UI level (`SettingsPage.tsx:70`).
- **60/60 tests green** for existing suites.

---

## Follow-up tasks for `/sdd:implement dayflow-planner`

1. **Storage error UX** — surface plain-language error when `openDatabase()` fails; link to backup import.
2. **Test tiers** — add component tests for UI ACs; e2e-through-UI harness (Playwright or equivalent) including AC-13 offline smoke; visual-regression snapshots for dashboard/long-term; load scripts for §6 NFR p95 targets.
3. **Stage-2 fixes** — tie-break, per-store merge dedup, backup record validation, step edit error handling, FSD boundary refactor, contract fixture test, minor unit test gaps.

## Deferred (spec §8)

- Post-install export hint (S1-08) — owner: PM, due: before ship.
- Empty-state recovery test (S1-09) — owner: Tech Lead, due: with e2e suite.

---

## Next gate

Re-run `/sdd:review dayflow-planner` after implement addresses fix-now items. Do **not** `/clear` — stay in context for the fix loop.
